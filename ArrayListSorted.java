import java.util.*;
public class ArrayListSorted{
  public class void main(String args[]){
    ArrayList<Sting> FoodList=new   ArrayList<Sting>();
      list.add=("gulabjamun");
      list.add=("sweet curd");
      list.add=("jalebi");
      list.add=("sandwich");
      list.add=("pasta");
    //for descending order using collection sort method with reverse order
    Collections.sort(FoodList,Collections.reverseOrder());
    //print the sorted array list
    System.out.print(FoodList);
    
  }
}
